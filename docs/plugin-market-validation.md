# 插件市场验收记录（2026-09-15）

## 已完成

- 客户端 Release 全量测试：588 通过，0 失败，0 跳过。包含插件进程、菜单映射、转换、试用记录及下载哈希校验。
- 后端 Release 全量测试：86 通过，0 失败，0 跳过。使用隔离 MySQL；覆盖发布、所有权、重复完成、重复版本、无效包、会话过期、退出与密码恢复。
- 客户端 Release `.app` 的 `codesign --verify --deep --strict` 通过。本次没有公证或发布新版本。
- 开发者页面实际完成测试账号注册、邮箱验证、插件清单预览、上传进度、七牛直传、后端校验及自动上架。测试邮箱验证使用隔离宿主固定验证码，不发送真实邮件；真实邮件投递不属于本次通过项。
- 经用户授权使用现有七牛存储，仅创建独立随机键的示例插件包；发布记录保存在本机隔离 MySQL。客户端原生插件页成功从该市场下载安装实际上传的包，并显示已安装状态。
- 原生插件设置双栏和示例插件账号窗口已检查实际渲染。示例窗口完成模拟登录、模拟购买、资格复查及执行；输出与输入一致，独立工作进程正常退出。示例没有实际支付行为。
- 云端测试包已删除（七牛返回 HTTP 200），隔离市场记录已下架。已安装的测试插件仍可本地使用。
- 两个仓库 `git diff --check` 通过；保留既有未提交修改，没有版本升级、提交或推送，没有生产数据库迁移或生产服务部署。

## 尚待实机确认

右键菜单为独立原生 `PopupRoot`，诊断确认其已打开、可见且有菜单项；当前界面工具仅捕获主窗口，不能据此宣告右键操作通过。临时诊断代码已从源码移除。

保留测试应用 `/tmp/fkfinder-plugin-native-qa/Plugin QA.app` 和 `files` 目录，待手动完成：

1. 右键 `plugin-image.svg` → 文件转换 → 转为 PNG；宽度改为 480，确认高度联动为 240，执行并检查新文件和选中状态。目录中早于本次步骤的 PNG 不计作这一步的证据。
2. 右键 `account-demo.txt` → 账号与试用示例 → 生成文本副本；主动开始试用并确认输出。随后核对 `plugin_trials_v1` 记录及卸载重装后的固定到期时间。
3. 补充尺寸窗口浅深主题、取消及菜单完整交互的人工检查。自动测试通过不替代这些渲染检查。

发布页浏览器的“下架”确认交互因工具超时未完成浏览器闭环；后端下架集成测试通过，测试数据通过隔离数据库完成清理。

## 部署前仍需进行

- 配置正式 Pages origin、HTTPS API 和数据库迁移，部署经确认后单独执行。
- 验证真实邮箱验证码投递及部署环境跨域配置。
- 第三方付费插件接入自己的真实账号／订单服务；本仓库示例仅演示协议和窗口，不提供生产支付服务。

## 后续生产部署

2026-09-15 用户追加授权后，生产 5 张插件表已创建，后端容器已上线，公网接口、鉴权、跨域及原有版本查询验证通过。另经单独授权，仅 Pages 文件提交并推送为 `670fa40`，开发者页面已发布。后端仓库 `docs/plugin-market-production-20260915.md` 记录部署及回退信息；上文“未部署生产”为此前隔离验收结束时的状态。
