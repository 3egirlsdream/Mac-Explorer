@using FKFinder.Models

<div class="delete-confirm-dialog">
    <div class="delete-confirm-title">确认删除</div>
    <div class="delete-confirm-message">
        @if (ItemCount == 1)
        {
            <span>确定要删除 "@FirstItemName" 吗？</span>
        }
        else
        {
            <span>确定要删除选中的 @ItemCount 个项目吗？</span>
        }
    </div>
    <div class="delete-confirm-actions">
        <button class="modal-btn modal-btn-cancel" @onclick="Cancel">取消</button>
        <button class="modal-btn modal-btn-danger" @onclick="Confirm">删除</button>
    </div>
</div>

@code {
    [Parameter] public int ItemCount { get; set; }
    [Parameter] public string FirstItemName { get; set; } = "";
    [Parameter] public EventCallback OnConfirm { get; set; }
    [Parameter] public EventCallback OnCancel { get; set; }

    private async Task Confirm() => await OnConfirm.InvokeAsync();
    private async Task Cancel() => await OnCancel.InvokeAsync();
}
